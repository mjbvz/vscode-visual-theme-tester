Open 6–8 files of different types (.ts, .json, .md, .css, .html, .py) so the editor tab bar is full with multiple tabs. Make one of them a "preview" tab (single-click in explorer) and one modified but unsaved.

Take a screenshot showing the tab bar with active, inactive, preview, and modified tabs visible, plus the editor content below.
