Open a file in the editor, then run "View: Split Editor Right" to create a side-by-side layout.

Open a second file of a different language (e.g. a TypeScript file and a CSS file) in the new group to show syntax highlighting contrast. Take a screenshot showing the split editor layout with both files visible.
