Set up a workspace with a realistic nested folder structure (src/, tests/, config/, docs/) with multiple file types (.ts, .json, .md, .css, .html).

Open the Explorer sidebar and expand several folders so the tree shows 15–20 items. Have one file open in the editor. Take a screenshot showing the Explorer tree with expanded folders and file icons.
