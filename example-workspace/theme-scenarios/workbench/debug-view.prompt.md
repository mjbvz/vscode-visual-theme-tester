Create a simple TypeScript or JavaScript file with a few functions.

Open the Run and Debug sidebar using "View: Show Run and Debug". Set a breakpoint on a line by running "Debug: Toggle Breakpoint" after placing the cursor on a line.

Take a screenshot showing the Debug sidebar with its sections (Variables, Watch, Call Stack, Breakpoints) and the editor with a visible breakpoint marker in the gutter.
