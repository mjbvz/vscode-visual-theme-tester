Set up a workspace with a git repository that has staged changes, unstaged changes, and untracked files.

Open the Source Control sidebar using "View: Show Source Control". The sidebar should show file lists under different sections (Staged Changes, Changes). Take a screenshot showing the Source Control view with visible change indicators.
