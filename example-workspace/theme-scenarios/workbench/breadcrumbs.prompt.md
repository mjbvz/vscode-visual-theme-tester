Open a TypeScript file inside a nested folder (e.g. src/utils/helpers.ts) that has classes and functions. Make sure breadcrumbs are enabled by running "View: Toggle Breadcrumbs" if needed.

Click on or focus a function inside the file so the breadcrumb trail shows the full path: folder > file > class > function. Take a screenshot showing the breadcrumb bar at the top of the editor.
