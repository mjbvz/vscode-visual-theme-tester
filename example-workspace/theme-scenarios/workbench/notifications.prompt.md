Open a file in the editor. Trigger a few notifications — for example, run an unknown command to get an error notification.

Run "Notifications: Show Notifications" to open the notification center. Take a screenshot showing notification toast(s) or the notification center overlay.
