Open a file in the editor. Open the Command Palette with the keyboard shortcut.

Type "format" so the palette shows filtered commands. Do NOT press Enter — leave the palette open. Take a screenshot showing the Command Palette dropdown with filtered results.
