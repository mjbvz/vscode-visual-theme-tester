Open a Jupyter notebook (.ipynb) that has a few Python cells with code, a markdown cell with a heading, and at least one cell that has text output and one that has a simple table or list output already saved in the notebook.

Take a screenshot showing the notebook with visible cells and outputs.
