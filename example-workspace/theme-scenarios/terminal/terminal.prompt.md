Open a source file in the editor. Then open the integrated terminal using "View: Toggle Terminal". Wait for the terminal to initialize.

Run a few safe basic commands (like echo, ls -la, node --version, env | head) so the terminal has a screen's worth of output visible. Take a screenshot showing the editor and terminal panel together.
