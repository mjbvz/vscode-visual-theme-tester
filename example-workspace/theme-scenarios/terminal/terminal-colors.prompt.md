Open a source file in the editor.

Open the integrated terminal using "View: Toggle Terminal". Wait for the terminal to initialize.

Run commands that produce colorful output using ANSI escape codes. For example, use printf or echo with escape sequences to print text in red, green, yellow, blue, magenta, cyan, and white — both normal and bold variants. Also include a few lines with colored backgrounds. The goal is to fill the terminal with a variety of ANSI colors so all terminal color theme tokens are visible.

Take a screenshot showing the editor and terminal with colorful output.
