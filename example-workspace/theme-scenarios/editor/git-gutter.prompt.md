Set up a workspace with a git repository that has a committed TypeScript file. Modify the file so there are added lines (green gutter), changed lines (blue gutter), and a section where lines were deleted (red triangle).

The gutter decorations should be visible in the editor margin. Take a screenshot showing the editor with git change indicators in the gutter.
