Open a TypeScript file with several functions that have JSDoc comments.

Place the cursor on a function call and run the "Show Hover" command. Wait for the hover tooltip to appear. Take a screenshot showing the hover widget with type information.
