Open a long TypeScript file (80+ lines) with varied content — imports, classes, functions, comments, string literals.

Make sure the minimap is visible on the right side of the editor. Scroll to the middle of the file so the minimap shows the viewport highlight. Take a screenshot showing the editor with the minimap clearly visible.
