Open a TypeScript file that has several occurrences of a common word (e.g. a function name used multiple times).

Focus the editor, then open the Find widget using "Find". Type the search term into the find input. Wait for the matches to be highlighted in the editor. Take a screenshot showing the find widget with match count and highlighted matches visible in the editor.
