Open a TypeScript file that calls functions defined in another file.

Place the cursor on a function call and run "Peek Definition". Wait for the peek widget to appear inline in the editor. Take a screenshot showing the peek view with the definition visible inside the inline widget.
