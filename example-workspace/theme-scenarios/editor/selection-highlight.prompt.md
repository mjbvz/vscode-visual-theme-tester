Open a TypeScript file with a variable name that appears multiple times.

Select one occurrence of the variable by double-clicking it (or use "Add Selection To Next Find Match" command a few times to create multiple cursors). Take a screenshot showing the editor with the selection highlighted and all other occurrences highlighted in a different color.
