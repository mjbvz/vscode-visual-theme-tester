Open a TypeScript file that imports a module with several exported members.

Place the cursor at the end of a line where a method would be called on an object (e.g. after typing a dot). Trigger the suggest widget. Wait for the suggest widget to appear. Take a screenshot showing the autocomplete dropdown.
