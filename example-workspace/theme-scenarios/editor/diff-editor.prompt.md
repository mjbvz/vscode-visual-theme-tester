Create two versions of a TypeScript file with meaningful differences — added lines, removed lines, and modified lines.

Open the diff editor by running "File: Compare Active File With..." or set up two files and use the diff view. The diff should show green (additions), red (deletions), and modified sections clearly. Take a screenshot showing the side-by-side diff editor with visible changes.
